/** @deprecated 建议 `import { notify } from "@/lib/notifyHub"` — 此处保留兼容旧路径 */
export {
  notify,
  notifySuccess,
  notifyError,
  notifyInfo,
  notifyLoading,
  resolveNotifyText,
  type BilingualInput,
} from "@/lib/notifyHub";
