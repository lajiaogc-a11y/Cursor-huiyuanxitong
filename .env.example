# 前端构建 / 本地开发（数据与登录走同源或 VITE_API_BASE 指向的 Node API + MySQL）

# 员工端 / 会员端：若静态站点与 API 不同源，必须设置（无尾部斜杠）
# VITE_API_BASE=https://你的API根地址
#
# 同源部署（Nginx 将 /api 反代到 Node）可留空，浏览器使用相对路径 /api/

# 本地 Vite 开发端口（默认 8081）
# VITE_DEV_PORT=8081

# 站点模式（会员/员工域名，逗号分隔）
# VITE_MEMBER_HOSTS=crm.fastgc.cc
# VITE_STAFF_HOSTS=admin.crm.fastgc.cc

# AWS EC2 部署（deploy-full.ps1 / deploy-remote-node-api.ps1）
# DEPLOY_PM2_SSH=ubuntu@你的公网IP
# DEPLOY_REMOTE_DIR=/var/www/gc-app
# 私钥：目录内第一个 .pem，或显式 .pem 路径（须为 OpenSSH 格式，PuTTY .ppk 需先转换）
# DEPLOY_SSH_KEY_DIR=D:\亚马逊密匙
# DEPLOY_SSH_IDENTITY_FILE=D:\亚马逊密匙\your-key.pem
