/**
 * 会员端签到展示数据（全部后端计算，前端只展示）
 * Re-exports `./service.js` for stable import paths.
 */
export type { MemberCheckInDailySnapshot } from './service.js';
export { buildMemberCheckInDailySnapshot } from './service.js';
